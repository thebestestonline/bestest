

<div class="container">
    <div class="row">
        <div class="col-sm-2 text-center">
            <%= image_tag @b.avatar.url, :class => "avatar-show" %>
        </div>
        <div class="col-sm-10">
            <h1>The Best <%= @b.term %></h1>
            <h5><%= link_to @b.company_name, @b.web_address %></h5>
            <br>
            <h4><%= @b.short_description %></h4>
            <br>
            <p class="info-p"><%= @b.long_description %></p>
        </div>
    </div>
</div>